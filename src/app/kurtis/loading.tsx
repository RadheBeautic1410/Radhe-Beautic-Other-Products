import { PageLoader, ContentSkeleton } from '@/components/page-loader'
import { SiteHeader } from '@/components/site-header'

export default function KurtisLoading() {
  return (
    <>
      {/* <SiteHeader />
      <main className="min-h-screen">
        <div className="max-w-7xl mx-auto px-4 py-8">
          <div className="mb-8 space-y-4">
            <div className="h-10 bg-primary/20 rounded-lg w-1/4 animate-pulse"></div>
            <div className="h-6 bg-primary/10 rounded-lg w-1/2 animate-pulse"></div>
          </div>
          
          <div className="mb-8 flex flex-wrap gap-4">
            {[1, 2, 3, 4].map((i) => (
              <div key={i} className="h-10 bg-primary/10 rounded-lg w-24 animate-pulse"></div>
            ))}
          </div>
          <ContentSkeleton count={8} />
        </div>
      </main> */}
    </>
  )
}

